@{
    var viewName = System.IO.Path.GetFileNameWithoutExtension(Path);
    ViewBag.Tittle = "Proyectos";
    ViewBag.IconTittle = "fa fa-calendar-check-o";
}

<div id="@viewName">
    <!-- Scripts -->
    <script src="@Url.Content(App.Utils.Utils.GetJsFilePath(Path))"></script>
    <!-- Plantilla que se utilizará en la columna de acciones de la grilla -->
    <script id="actionTemplate" type="text/x-kendo-template">
        <div class='text-center'>
            <a class='btn btn-primary' data-modal="modal-xl" href='@Url.Action("UpdProyecto", "Proyectos", new { area = "TRANS" })/#=data.Id#'>Editar</a>
            <a class='ml-2 btn btn-primary' data-modal="modal-xl" href='@Url.Action("RubrosProyecto", "Proyectos", new { area = "TRANS" })/#=data.Id#'>Editar rubros</a>
        </div>
    </script>


    <div class="container-fluid ">

        <div class="card">
            <div class="card-header">
                <div class="h5 m-0">
                    Listado de Proyectos <label class="float-right mt-0 mb-0">
                        @Html.ActionLink("Registrar", "InsProyecto", "Proyectos", new { area = "TRANS" }, new { @data_modal = "modal-xl", @class = "btn btn-primary float-right" })
                    </label>
                </div>

            </div>
            <div class="card-body">
                @(Html.Kendo().Grid<Proyecto>()
                    .Name("grid")
                    .Columns(columns =>
                    {
                        columns.Bound(p => p.Id).Hidden();
                        columns.Bound(p => p.Codigo).Title("Codigo").Width(100);
                        columns.Bound(p => p.Descripcion).Title("Descripcion").Width(130);
                        columns.Bound(p => p.PresupuestoAsignado).Title("Presupuesto asignado").Width(130);
                        columns.Bound(c => c.Id).ClientTemplate("#=Index.actionTemplate(data)#").Title("Acciones").Filterable(false).Width(130);
                    })
                    .Pageable()
                    .Sortable()
                    .Filterable(ftb => ftb.Mode(GridFilterMode.Row))
                    .Scrollable()
                    .Events(events => events.DataBound("Site.handleModal"))
                    .HtmlAttributes(new { style = "height:550px;" })
                    .DataSource(dataSource => dataSource
                        .Ajax()
                        .Read(read => read.Action("GetListProyectos", "Proyectos"))
                        .Events(e => e.Error("Site.handleError"))
                    )
                )
            </div>
        </div>
    </div>
    <!-- Inicializadores -->
    <script>
        $(function () {
            @(viewName).init();
        });

    </script>
</div>
